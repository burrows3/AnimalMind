{
  "name": "animalmind",
  "version": "0.1.0",
  "description": "Autonomous animal health research agents — data ingest and app",
  "private": true,
  "scripts": {
    "ingest": "node scripts/ingest-data-sources.js",
    "report": "node scripts/write-report.js",
    "test-live": "node scripts/ingest-data-sources.js && node scripts/write-report.js",
    "audit": "npm audit",
    "start": "node server.js",
    "build:ui": "cd frontend && npm run build",
    "deploy:ui": "npm run build:ui && node scripts/copy-build.js",
    "repurpose": "node scripts/repurpose-run.js",
    "repurpose:fixtures": "REPURPOSE_USE_FIXTURES=1 node scripts/repurpose-run.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.6.0",
    "express": "^4.21.0"
  }
}
